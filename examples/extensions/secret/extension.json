{
  "name": "secret",
  "version": "1.0.0",
  "exec": "./secret",
  "language": "go",
  "description": "collects secrets from the user via a masked panel; the model never sees the value",
  "enabled": true,
  "data_secrets": false
}
