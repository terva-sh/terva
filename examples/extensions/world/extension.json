{
  "name": "world",
  "version": "1.0.0",
  "exec": "go",
  "args": ["run", "."],
  "language": "go",
  "description": "a generic procedural world the agent explores through tools — the 'extension as a world' pattern, with a bundled explorer persona",
  "enabled": true,
  "config": [
    {
      "key": "seed",
      "label": "World seed",
      "type": "string",
      "default": "wayfarer",
      "description": "Seeds the procedural land. Same seed = same world."
    }
  ],
  "data_secrets": false
}
