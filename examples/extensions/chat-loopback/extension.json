{
  "name": "chat-loopback",
  "version": "1.0.0",
  "exec": "go",
  "args": ["run", "."],
  "language": "go",
  "description": "one process, both roles: a filesystem-backed chat connector (drop a file in inbox/ → an agent turn) that ALSO serves a tool reading the same live state",
  "enabled": true,
  "connector": true,
  "data_secrets": false
}
