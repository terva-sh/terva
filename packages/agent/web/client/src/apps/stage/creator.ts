// The creator persona: Kartoittaja (🧭), the cartographer. A card-less immersive
// chat bound to it is the "start something new" workshop — Stage's sixth entry
// point, the on-ramp for someone with an idea and no session yet
// (docs/proposals/creator.md, C1). The session carries no card, so the Chat
// keys its grounding empty-state and title on this persona id, and the Library
// door creates the session with it.
export const CREATOR_PERSONA = 'kartoittaja'
